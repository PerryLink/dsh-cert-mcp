node_modules/
*.tgz
*.log

# The gate chain (typecheck / typecheck:ci / tests) installs with pnpm, and
# pnpm-workspace.yaml keeps this repo its own workspace root; the lockfile is
# tracked so CI installs the exact reviewed graph.
